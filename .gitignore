# build output
dist/
.output/

# Astro cache
.astro/

# dev dependencies
node_modules/

# logs
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*

# env
.env
.env.local
.env.*.local

# editor
.vscode/
.idea/
.DS_Store
*.swp
*~

# os
Thumbs.db
desktop.ini

# claude code agent settings (local-only, not for the repo)
.claude/

# planning docs / scratch
.planning/
.scratch_*/

# build archives (Hostinger deploys)
dist_*.zip

# scratch commit-message buffers used by Windows tooling (PowerShell
# here-strings can't be piped reliably to git commit -m, so we write
# the message to a temp file and pass it via -F). Never want this in
# the repo.
.commit-msg.tmp

