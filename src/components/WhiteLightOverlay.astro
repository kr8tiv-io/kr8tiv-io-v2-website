---
/**
 * White-light overlay + quote. Hidden by default; JS adds `.flash`,
 * `.show`, `.release` classes when the footer button / `W` key fires.
 */
---
<div class="white-light-overlay" id="white-light-overlay" aria-hidden="true"></div>
<div class="white-light-text" id="white-light-text" aria-hidden="true">Every spectrum begins with white.</div>
